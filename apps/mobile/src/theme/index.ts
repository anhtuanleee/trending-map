export { colors, radius, spacing } from '@trending-map/ui-tokens';

export const mapLayout = {
  defaultCenter: [106.701, 10.776] as [number, number],
  defaultZoom: 13.2,
  minimumZoom: 9,
} as const;
